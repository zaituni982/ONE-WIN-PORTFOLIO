# Ultimate Digital Portfolio 🚀

A high-performance, dark-themed personal portfolio website designed for creative professionals. This project features a cinematic video background, 3D interactive elements, and a sleek neon aesthetic—built entirely with **Vanilla HTML, CSS, and JavaScript**.

**Current Profile:** Mohamed Zaituni (AI Specialist, Designer, Marketer)

## ✨ Key Features

* **🎬 Cinematic Hero:** Full-screen video background with an overlay.
* **⚡ Zero Dependencies:** No frameworks (React/Bootstrap) required. Extremely fast load times.
* **💎 Glassmorphism:** Translucent navigation bar with blur effects.
* **🃏 3D Flip Cards:** Interactive skill cards that rotate 180° on hover.
* **⌨️ Typing Animation:** dynamic text effect in the hero section (JavaScript).
* **📱 Fully Responsive:** Uses CSS Grid (`minmax`) to adapt to any screen size automatically.
* **✨ Preloader:** Custom animated pulse loader before the site reveals itself.

## 🛠️ Technologies Used

* **HTML5:** Semantic structure.
* **CSS3:** Flexbox, Grid, Keyframe Animations, Backdrop Filter.
* **JavaScript (ES6):** Typing logic and DOM manipulation.

## 🚀 Getting Started

### Prerequisites
You only need a web browser (Chrome, Firefox, Safari, Edge) to run this.

### Installation
1.  Download this repository or copy the code.
2.  Save the main file as `index.html`.
3.  Double-click `index.html` to open it in your browser.

## 🎨 Customization Guide

Since this is a single-file project, all edits happen inside `index.html`.

### 1. Changing the Video Background
Locate the `<video>` tag inside the `.hero` div. Change the `src` attribute to a local file or another direct video link.

```html
<video autoplay muted loop>
  <source src="my-video.mp4">
</video>